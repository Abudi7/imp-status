{% extends 'base.html.twig' %}

{% block title %}Add New System{% endblock %}

{% block body %}
    <h3 class="text-center rounded-pill bg-info text-white">Add New System</h3>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.info) }}
        {{ form_row(form.active) }}
        {# {{ form_row(form.createdAt) }} #}
    {{ form_end(form) }}
{% endblock %}
