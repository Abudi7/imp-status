{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Template</h1>

    {{ form_start(form) }}

    {{ form_row(form.template) }}
    

    {{ form_widget(form.save) }}

    {{ form_end(form) }}
{% endblock %}
